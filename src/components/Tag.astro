---
const { name, lowercase = false} = Astro.props;
const formattedName = lowercase ? name.toLowerCase() : name;
---

<span class="text-tag-text bg-tag-bg px-3 py-1 text-xs rounded-md font-bold">{formattedName}</span>